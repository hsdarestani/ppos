(()=>{const menu=document.getElementById('menuBtn'),nav=document.getElementById('mobileNav');if(menu&&nav)menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.fav').forEach(btn=>btn.addEventListener('click',async e=>{e.preventDefault();const id=btn.dataset.item;try{const r=await fetch(`/s/${window.VS.slug}/favorite/${id}`,{method:'POST',headers:{'X-Requested-With':'fetch'}});const d=await r.json();if(r.status===401&&d.login){location.href=d.login;return}if(d.ok)btn.classList.toggle('active',d.active)}catch(_){}}));document.querySelectorAll('form.request-card').forEach(form=>{form.addEventListener('submit',()=>{const b=form.querySelector('button[type="submit"]');if(b){b.disabled=true;b.dataset.old=b.innerHTML;b.innerHTML='در حال ثبت…'}})});})();